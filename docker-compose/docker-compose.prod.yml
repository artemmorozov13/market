version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ${PWD}/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ${PWD}/certificates:/etc/nginx/certificates
    depends_on:
      - backend
      - admin
      - app
      - landing
    restart: unless-stopped

  backend:
    build:
      context: ../
      dockerfile: docker/backend.prod.Dockerfile
    environment:
      - NODE_ENV=production
    env_file:
      - ../.env
    restart: unless-stopped

  admin:
    build:
      context: ../
      dockerfile: docker/admin.prod.Dockerfile
    environment:
      - NODE_ENV=production
    env_file:
      - ../.env
    restart: unless-stopped

  app:
    build:
      context: ../
      dockerfile: docker/app.prod.Dockerfile
    environment:
      - NODE_ENV=production
    env_file:
      - ../.env
    restart: unless-stopped

  landing:
    build:
      context: ../
      dockerfile: docker/landing.prod.Dockerfile
    ports:
      - "8080:80"  # Для тестирования
    restart: unless-stopped

volumes:
  landing-static: